import random

EMOJI = {
    "ok": "✅",
    "pin": "📌",
    "delete": "🗑" ,
    "search": "🔎",
    "hello": "👑",
    "thinking": "🤔",
}

LOADING_PHRASES = [
    "ща ща.. я уже работаю",
    "занимаюсь твоей командой",
    "опа! жди, сейчас всё будет",
    "секунду, думаю...",
    "уже кумекаю",
    "ок, момент",
    "принято, обрабатываю",
    "почти готово",
]

def random_loading_phrase() -> str:
    return random.choice(LOADING_PHRASES)

def start_text() -> str:
    return f"бот запущен {EMOJI['hello']}"

def ping_text() -> str:
    return "pong"

def help_text() -> str:
    return (
        "📋 *Доступные команды:*\n\n"
        "/start — запустить бота\n"
        "/ping — получить мотивационное сообщение прямо сейчас\n"
        "/nextday — перенести задачу на следующий день\n"
        "/ideapost — новая идея для поста\n"
        "/ideacontent — идея для съёмки контента\n"
        "/ideavideo — идея для кружочка\n"
        "/com — все команды бота\n"
        "/status — проверить статус бота\n\n"
        "📌 *Текстовые команды:*\n"
        "• `план на день` — задача на сегодня из расписания\n"
        "• `добавь в напоминания: текст` — создать напоминание\n"
        "• `удали напоминание 2` — удалить по номеру\n"
        "• `обучись: текст` — запомнить факт о тебе\n"
        "• `забудь старое` — очистить старые факты\n"
        "• `напомни через X минут: текст` — отложенное сообщение\n"
        "• `календарь` — заглушка (в разработке)\n\n"
        "🔥 Обычное сообщение без команд — получишь жёсткую поддержку."
    )

def unknown_command_text() -> str:
    return f"не понял команду {EMOJI['thinking']}"
